// client/src/app/(dashboard)/student-dashboard/components/NotificationBell.tsx
// Re-exports the real shared NotificationBell so the Navbar import path stays unchanged.
export { default } from '../../../../../components/NotificationBell';
